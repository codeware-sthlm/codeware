{
  "$schema": "https://json-schema.org/schema",
  "title": "Add Nx AI Plugin to a Project",
  "description": "Add the `@cdwr/nx-ai` plugin to a project by wiring up the analyze executor.",
  "type": "object",
  "properties": {
    "project": {
      "description": "The name of the project to add the analyze executor to.",
      "type": "string",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "Which project would you like to add the AI analyzer to?",
      "x-priority": "important"
    },
    "targetName": {
      "description": "Name of the target to add to the project.",
      "type": "string",
      "default": "analyze"
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false
    }
  },
  "required": ["project"]
}
